spring:
  cloud:
    config:
      name: config-client
#      uri: http://localhost:12111/
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:11111/eureka/